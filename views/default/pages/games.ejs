<%- include('../header.ejs', { page }) %>

    <%- include('../modules/sidebar.block.ejs', { page }) %>

    <%- include('../modules/header.block.ejs', { page }) %>

    <div system-wrapper>
        <% if (listType === 'category' && bannerImage) { %>
        <!-- Category Banner with Image -->
        <div class="relative bg-gradient-to-r from-gray-900 to-gray-800 text-white overflow-hidden">
            <!-- Category Image Background -->
            <% 
                let bannerImageUrl = bannerImage;
                // Check if it's the new JSON format
                try {
                    if (typeof bannerImage === 'string' && bannerImage.startsWith('{')) {
                        let parsedData = JSON.parse(bannerImage);
                        if (parsedData.webp && parsedData.webp.large) {
                            bannerImageUrl = `/${parsedData.webp.large.relativePath}`;
                        } else if (parsedData.original && parsedData.original.large) {
                            bannerImageUrl = `/${parsedData.original.large.relativePath}`;
                        }
                    } else {
                        bannerImageUrl = bannerImage.startsWith('/') ? bannerImage : '/' + bannerImage;
                    }
                } catch(e) {
                    bannerImageUrl = bannerImage.startsWith('/') ? bannerImage : '/' + bannerImage;
                }
            %>
            <div class="absolute inset-0 bg-center bg-cover" style="background-image: url('<%= bannerImageUrl %>')"></div>
            <!-- Gradient overlay that blends with the image -->
            <div class="absolute inset-0" style="background: linear-gradient(135deg, <%= bannerColor %>CC 0%, rgba(0,0,0,0.7) 50%, <%= bannerColor %>99 100%)"></div>
            
            <!-- Content layer -->
            <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-8 py-12">
                <div class="text-center">
                    <h1 class="text-4xl lg:text-6xl font-bold text-white drop-shadow-lg mb-4">
                        <%= listTitle %>
                    </h1>
                    <p class="text-xl lg:text-2xl text-white/90 drop-shadow mb-6">
                        <%= listDescription %>
                    </p>
                    <% if (games && games.length > 0) { %>
                    <p class="text-lg text-white/80 drop-shadow">
                        <%= games.length %> <%= games.length === 1 ? __('common.game') : __('common.games') %> <%= __('common.available_to_play') %>
                    </p>
                    <% } %>
                </div>
            </div>
        </div>
        <% } else if (listType === 'search') { %>
        <!-- Search Header with Background -->
        <div class="relative bg-gradient-to-r from-gray-900 to-gray-800 text-white overflow-hidden">
            <% if (games && games.length > 0 && games[0] && games[0].thumbnail) { %>
            <!-- First Game's Thumbnail as Background -->
            <% 
                let searchBannerUrl = games[0].thumbnail;
                // Check if it's the new JSON format
                try {
                    if (typeof games[0].thumbnail === 'string' && games[0].thumbnail.startsWith('{')) {
                        let parsedData = JSON.parse(games[0].thumbnail);
                        if (parsedData.webp && parsedData.webp.large) {
                            searchBannerUrl = `/${parsedData.webp.large.relativePath}`;
                        } else if (parsedData.original && parsedData.original.large) {
                            searchBannerUrl = `/${parsedData.original.large.relativePath}`;
                        }
                    } else {
                        searchBannerUrl = games[0].thumbnail.startsWith('/') ? games[0].thumbnail : '/' + games[0].thumbnail;
                    }
                } catch(e) {
                    searchBannerUrl = games[0].thumbnail.startsWith('/') ? games[0].thumbnail : '/' + games[0].thumbnail;
                }
            %>
            <div class="absolute inset-0 bg-center bg-cover" style="background-image: url('<%= searchBannerUrl %>')"></div>
            <!-- Gradient overlay that blends with the image -->
            <div class="absolute inset-0" style="background: linear-gradient(135deg, #6366F1CC 0%, rgba(0,0,0,0.8) 50%, #6366F199 100%)"></div>
            <% } else { %>
            <!-- Fallback gradient background -->
            <div class="absolute inset-0" style="background: linear-gradient(135deg, #6366F1 0%, #8B5CF6 50%, #EC4899 100%)"></div>
            <% } %>
            
            <!-- Content layer -->
            <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-8 py-12">
                <div class="text-center">
                    <h1 class="text-4xl lg:text-6xl font-bold text-white drop-shadow-lg mb-4">
                        <%= __('search.results') %>
                    </h1>
                    <p class="text-xl lg:text-2xl text-white/90 drop-shadow mb-6">
                        <%= __('games.results_found', { count: totalCount || 0 }) %> "<span class="font-semibold text-white"><%= searchQuery %></span>"
                    </p>
                    <% if (games && games.length > 0) { %>
                    <p class="text-lg text-white/80 drop-shadow">
                        <%= __('games.showing_results', { start: ((currentPage - 1) * gamesPerPage) + 1, end: ((currentPage - 1) * gamesPerPage) + games.length, total: totalCount }) %>
                    </p>
                    <% } else { %>
                    <p class="text-lg text-white/80 drop-shadow">
                        <%= __('games.try_different_search') %>
                    </p>
                    <% } %>
                </div>
            </div>
        </div>
        <% } else { %>
        <%- include('../modules/banner.block.ejs', { page }) %>
        <% } %>
        
        <!-- Main Content -->
        <main class="max-w-6xl mx-auto px-4 sm:px-8 mt-6 space-y-8">
            <!-- Header Banner Ad -->
            <div class="flex justify-center">
                <% if (enable_frontend_pjax === '0') { %>
                    <!-- Direct ads for AdSense compatibility when PJAX is disabled -->
                    <%- include('../modules/ad_direct.block.ejs', { placement: 'header-banner', width: 728, height: 90, classes: '!hidden lg:!block mx-auto' }) %>
                    <!-- Mobile header ad (sidebar rectangle) -->
                    <%- include('../modules/ad_direct.block.ejs', { placement: 'sidebar-rectangle', width: 300, height: 250, classes: '!block lg:!hidden mx-auto' }) %>
                <% } else { %>
                    <!-- AJAX ads for smooth navigation when PJAX is enabled -->
                    <%- include('../modules/ad_container.block.ejs', { placement: 'header-banner', width: 728, height: 90, classes: '!hidden lg:!block mx-auto' }) %>
                    <!-- Mobile header ad (sidebar rectangle) -->
                    <%- include('../modules/ad_container.block.ejs', { placement: 'sidebar-rectangle', width: 300, height: 250, classes: '!block lg:!hidden mx-auto' }) %>
                <% } %>
            </div>
            <!-- Popular Tags -->
            <div class="bg-white dark:bg-gray-800 rounded-2xl border border-gray-200 dark:border-gray-700 shadow-sm p-4 flex flex-col gap-4">
                <div class="flex items-center justify-between mb-2">
                    <h2 class="font-semibold text-gray-900 dark:text-gray-100 text-lg flex items-center gap-2">
                        <span class="text-2xl">🏷️</span>
                        <%= __('games.popular_tags') %>
                    </h2>
                    <div class="flex items-center gap-2">
                        <button class="p-2 rounded-full hover:bg-gray-100 dark:hover:bg-gray-700 transition hidden sm:block"
                            id="tags-left">
                            <svg class="w-5 h-5 text-gray-400 dark:text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                            </svg>
                        </button>
                        <button class="p-2 rounded-full hover:bg-gray-100 dark:hover:bg-gray-700 transition hidden sm:block"
                            id="tags-right">
                            <svg class="w-5 h-5 text-gray-400 dark:text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                            </svg>
                        </button>
                    </div>
                </div>
                <div class="flex gap-3 overflow-x-auto scrollbar-none pb-1" id="tags-scroll">
                    <% if (popularTags && popularTags.length > 0) { %>
                        <% 
                        const tagColors = [
                            { class: 'tag-blue', icon: '🚗' },
                            { class: 'tag-green', icon: '🎮' },
                            { class: 'tag-purple', icon: '⚽' },
                            { class: 'tag-pink', icon: '🏍️' },
                            { class: 'tag-yellow', icon: '✈️' },
                            { class: 'tag-red', icon: '🔫' },
                            { class: 'tag-indigo', icon: '🧩' },
                            { class: 'tag-cyan', icon: '🏁' },
                            { class: 'tag-emerald', icon: '🌟' },
                            { class: 'tag-orange', icon: '🎯' },
                            { class: 'tag-teal', icon: '🚀' },
                            { class: 'tag-rose', icon: '⚔️' }
                        ];
                        %>
                        <% popularTags.forEach((tag, index) => { %>
                            <% const colorScheme = tagColors[index % tagColors.length]; %>
                            <a href="/games/tag/<%= encodeURIComponent(tag.tag) %>" 
                               class="flex items-center <%= colorScheme.class %> px-4 py-2.5 rounded-full font-medium text-sm whitespace-nowrap gap-2 border transition-all duration-200 hover:shadow-sm hover:-translate-y-0.5" 
                               system-nav>
                                <span class="text-base"><%= colorScheme.icon %></span>
                                <span class="capitalize"><%= tag.tag %></span>
                                <span class="bg-white/20 dark:bg-black/20 px-2 py-0.5 rounded-full text-xs font-semibold">
                                    <%= tag.count %>
                                </span>
                            </a>
                        <% }) %>
                    <% } else { %>
                        <!-- Fallback static tags if no dynamic tags available -->
                        <a href="/games/tag/action" class="flex items-center bg-blue-50 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 px-4 py-2.5 rounded-full font-medium text-sm whitespace-nowrap gap-2 border border-blue-100 dark:border-blue-800 hover:bg-blue-100 dark:hover:bg-blue-900/50 transition-all duration-200 hover:shadow-sm hover:-translate-y-0.5" system-nav><span>⚡</span><%= __('categories.action') %></a>
                        <a href="/games/tag/adventure" class="flex items-center bg-green-50 dark:bg-green-900/30 text-green-600 dark:text-green-400 px-4 py-2.5 rounded-full font-medium text-sm whitespace-nowrap gap-2 border border-green-100 dark:border-green-800 hover:bg-green-100 dark:hover:bg-green-900/50 transition-all duration-200 hover:shadow-sm hover:-translate-y-0.5" system-nav><span>🗺️</span><%= __('categories.adventure') %></a>
                        <a href="/games/tag/racing" class="flex items-center bg-red-50 dark:bg-red-900/30 text-red-600 dark:text-red-400 px-4 py-2.5 rounded-full font-medium text-sm whitespace-nowrap gap-2 border border-red-100 dark:border-red-800 hover:bg-red-100 dark:hover:bg-red-900/50 transition-all duration-200 hover:shadow-sm hover:-translate-y-0.5" system-nav><span>🏁</span><%= __('categories.racing') %></a>
                        <a href="/games/tag/puzzle" class="flex items-center bg-purple-50 dark:bg-purple-900/30 text-purple-600 dark:text-purple-400 px-4 py-2.5 rounded-full font-medium text-sm whitespace-nowrap gap-2 border border-purple-100 dark:border-purple-800 hover:bg-purple-100 dark:hover:bg-purple-900/50 transition-all duration-200 hover:shadow-sm hover:-translate-y-0.5" system-nav><span>🧩</span><%= __('categories.puzzle') %></a>
                    <% } %>
                </div>
            </div>

            <!-- Sort & Pagination (Top) -->
            <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
                <div class="flex items-center gap-2 w-full sm:w-auto">
                    <div class="relative">
                        <button id="sortDropdownBtn" 
                            class="flex items-center border border-gray-200 dark:border-gray-600 bg-white dark:bg-gray-700 rounded-xl px-4 py-2 text-gray-700 dark:text-gray-300 font-medium text-sm shadow-sm hover:bg-gray-50 dark:hover:bg-gray-600 transition-all duration-200 hover:shadow-md">
                            <svg class="w-5 h-5 mr-2 text-gray-400 dark:text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4h13M3 8h9m-9 4h9m0 0l-3-3m3 3l-3 3M3 16h6"></path>
                            </svg>
                            <span id="currentSort">
                                <% if (listType === 'top') { %><%= __('games.top_rated') %><% } else if (listType === 'popular') { %><%= __('games.most_popular') %><% } else if (listType === 'trending') { %><%= __('games.trending') %><% } else if (listType === 'featured') { %><%= __('games.featured') %><% } else if (listType === 'recent') { %><%= __('games.recently_played') %><% } else if (listType === 'favorites') { %><%= __('user.favorites.title') %><% } else { %><%= __('games.latest') %><% } %>
                            </span>
                            <svg class="w-4 h-4 ml-2 text-gray-400 dark:text-gray-500 transition-transform duration-200" id="sortDropdownIcon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </button>
                        
                        <!-- Dropdown Menu -->
                        <div id="sortDropdown" class="absolute top-full left-0 mt-2 w-56 bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-lg z-50 hidden">
                            <div class="py-2">
                                <a href="/games" class="flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors duration-200 <%= listType === 'all' ? 'bg-blue-50 dark:bg-blue-900/50 text-blue-600 dark:text-blue-400' : '' %>" system-nav>
                                    <svg class="w-5 h-5 mr-3 text-gray-400 dark:text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                    </svg>
                                    <div class="flex flex-col">
                                        <span class="font-medium"><%= __('games.latest') %></span>
                                        <span class="text-xs text-gray-500 dark:text-gray-400"><%= __('games.new_games') %></span>
                                    </div>
                                </a>
                                <a href="/games/popular" class="flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors duration-200 <%= listType === 'popular' ? 'bg-blue-50 dark:bg-blue-900/50 text-blue-600 dark:text-blue-400' : '' %>" system-nav>
                                    <svg class="w-5 h-5 mr-3 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10c0-2 .5-5 2.986-7C14 5 16.09 5.777 17.656 7.343A7.975 7.975 0 0120 13a7.975 7.975 0 01-2.343 5.657z"></path>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.879 16.121A3 3 0 1012.015 11L11 14l4 4-6.121-1.879z"></path>
                                    </svg>
                                    <div class="flex flex-col">
                                        <span class="font-medium"><%= __('games.most_popular') %></span>
                                        <span class="text-xs text-gray-500 dark:text-gray-400"><%= __('games.popular_games') %></span>
                                    </div>
                                </a>
                                <a href="/games/top" class="flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors duration-200 <%= listType === 'top' ? 'bg-blue-50 dark:bg-blue-900/50 text-blue-600 dark:text-blue-400' : '' %>" system-nav>
                                    <svg class="w-5 h-5 mr-3 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                                    </svg>
                                    <div class="flex flex-col">
                                        <span class="font-medium"><%= __('games.top_rated') %></span>
                                        <span class="text-xs text-gray-500 dark:text-gray-400"><%= __('games.top_rated') %></span>
                                    </div>
                                </a>
                                <a href="/games/trending" class="flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors duration-200 <%= listType === 'trending' ? 'bg-blue-50 dark:bg-blue-900/50 text-blue-600 dark:text-blue-400' : '' %>" system-nav>
                                    <svg class="w-5 h-5 mr-3 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                                    </svg>
                                    <div class="flex flex-col">
                                        <span class="font-medium"><%= __('games.trending') %></span>
                                        <span class="text-xs text-gray-500 dark:text-gray-400"><%= __('games.trending') %></span>
                                    </div>
                                </a>
                                <a href="/games/featured" class="flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors duration-200 <%= listType === 'featured' ? 'bg-blue-50 dark:bg-blue-900/50 text-blue-600 dark:text-blue-400' : '' %>" system-nav>
                                    <svg class="w-5 h-5 mr-3 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                                    </svg>
                                    <div class="flex flex-col">
                                        <span class="font-medium"><%= __('games.featured') %></span>
                                        <span class="text-xs text-gray-500 dark:text-gray-400"><%= __('games.handpicked_premium') %></span>
                                    </div>
                                </a>
                                <% if (user) { %>
                                <hr class="my-2 border-gray-100 dark:border-gray-700">
                                <a href="/games/recent" class="flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors duration-200 <%= listType === 'recent' ? 'bg-blue-50 dark:bg-blue-900/50 text-blue-600 dark:text-blue-400' : '' %>" system-nav>
                                    <svg class="w-5 h-5 mr-3 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                    </svg>
                                    <div class="flex flex-col">
                                        <span class="font-medium"><%= __('games.recently_played') %></span>
                                        <span class="text-xs text-gray-500 dark:text-gray-400"><%= __('games.played_recently_desc') %></span>
                                    </div>
                                </a>
                                <a href="/games/favorites" class="flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors duration-200 <%= listType === 'favorites' ? 'bg-blue-50 dark:bg-blue-900/50 text-blue-600 dark:text-blue-400' : '' %>" system-nav>
                                    <svg class="w-5 h-5 mr-3 text-pink-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                                    </svg>
                                    <div class="flex flex-col">
                                        <span class="font-medium"><%= __('user.favorites.title') %></span>
                                        <span class="text-xs text-gray-500 dark:text-gray-400"><%= __('games.favorited_desc') %></span>
                                    </div>
                                </a>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- Top Pagination -->
                <nav class="inline-flex items-center space-x-1" aria-label="Pagination">
                    <% if (currentPage > 1) { %>
                        <a href="<%= baseUrl %>/page/<%= currentPage - 1 %>" class="inline-flex items-center gap-2 rounded-full bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 border border-gray-300 dark:border-gray-600 font-medium" system-nav>
                            <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                            </svg> <%= __('pagination.previous') %>
                        </a>
                    <% } else { %>
                        <span class="inline-flex items-center gap-2 rounded-full bg-gray-100 dark:bg-gray-700 text-gray-400 dark:text-gray-500 px-4 py-2 font-medium">
                            <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                            </svg> <%= __('pagination.previous') %>
                        </span>
                    <% } %>
                    
                    <span class="px-3 py-2 text-gray-600 dark:text-gray-400"><%= __('pagination.page') %> <%= currentPage %></span>
                    
                    <% if (hasNextPage) { %>
                        <a href="<%= baseUrl %>/page/<%= currentPage + 1 %>" class="inline-flex items-center gap-2 rounded-full bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 border border-gray-300 dark:border-gray-600 font-medium" system-nav>
                            <%= __('pagination.next') %> <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                            </svg>
                        </a>
                    <% } else { %>
                        <span class="inline-flex items-center gap-2 rounded-full bg-gray-100 dark:bg-gray-700 text-gray-400 dark:text-gray-500 px-4 py-2 font-medium">
                            <%= __('pagination.next') %> <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                            </svg>
                        </span>
                    <% } %>
                </nav>
            </div>

            <% if (games && games.length > 0) { %>
            <!-- Sidebar Rectangle Ad (Mobile: before games, Desktop: in sidebar) -->
            <div class="lg:hidden flex justify-center mb-6">
                <% if (enable_frontend_pjax === '0') { %>
                    <!-- Direct ads for AdSense compatibility when PJAX is disabled -->
                    <%- include('../modules/ad_direct.block.ejs', { placement: 'sidebar-rectangle', width: 300, height: 250, classes: 'mx-auto' }) %>
                <% } else { %>
                    <!-- AJAX ads for smooth navigation when PJAX is enabled -->
                    <%- include('../modules/ad_container.block.ejs', { placement: 'sidebar-rectangle', width: 300, height: 250, classes: 'mx-auto' }) %>
                <% } %>
            </div>
            
            <!-- Games Grid -->
            <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4">
                <% games.forEach(game => { %>
                    <a href="/play/<%= game.slug %>" class="group" system-nav>
                        <div class="game-thumbnail aspect-square rounded-2xl overflow-hidden transition-all duration-200 group-hover:shadow-xl group-hover:-translate-y-1 relative" data-game-id="<%= game.id %>">
                            <% if (game.parsedThumbnail && game.parsedThumbnail.webp && game.parsedThumbnail.original) {
                                let webpImage = game.parsedThumbnail.webp.thumbnail || game.parsedThumbnail.webp.standard;
                                let originalImage = game.parsedThumbnail.original.thumbnail || game.parsedThumbnail.original.standard;
                                
                                if (webpImage && originalImage) { %>
                                    <picture>
                                        <source srcset="/<%= webpImage.relativePath %>" type="image/webp">
                                        <img src="/<%= originalImage.relativePath %>" alt="<%= game.title %>" class="w-full h-full object-cover" loading="lazy">
                                    </picture>
                                <% } else { %>
                                    <img src="<%= game.thumbnail && game.thumbnail.startsWith('/') ? game.thumbnail : '/' + (game.thumbnail || '') %>" alt="<%= game.title %>" class="w-full h-full object-cover" loading="lazy">
                                <% }
                            } else if (game.thumbnail) { %>
                                <img src="<%= game.thumbnail.startsWith('/') ? game.thumbnail : '/' + game.thumbnail %>" alt="<%= game.title %>" class="w-full h-full object-cover" loading="lazy">
                            <% } else { %>
                                <img src="https://dummyimage.com/200x200/9CA3AF/FFFFFF?text=<%= encodeURIComponent(game.title) %>" alt="<%= game.title %>" class="w-full h-full object-cover" loading="lazy">
                            <% } %>
                            <div class="absolute inset-0 bg-black bg-opacity-0 group-hover:bg-opacity-60 transition-all duration-300 flex flex-col items-center justify-between p-3">
                                <!-- Play Icon -->
                                <div class="flex-1 flex items-center justify-center">
                                    <svg class="w-8 h-8 text-white opacity-0 group-hover:opacity-100 transition-opacity duration-300" fill="currentColor" viewBox="0 0 24 24">
                                        <path d="M8 5v14l11-7z"/>
                                    </svg>
                                </div>
                                <!-- Game Info at Bottom -->
                                <div class="text-center text-white opacity-0 group-hover:opacity-100 transition-opacity duration-300 transform translate-y-2 group-hover:translate-y-0">
                                    <% if (game.tags && game.tags.trim()) { %>
                                    <div class="flex flex-wrap gap-1 justify-center mb-2">
                                        <% game.tags.split(',').slice(0, 3).forEach(tag => { %>
                                            <span class="text-xs bg-white/20 px-2 py-1 rounded-full"><%= tag.trim() %></span>
                                        <% }); %>
                                    </div>
                                    <% } %>
                                    <h3 class="font-bold text-sm line-clamp-2"><%= game.title %></h3>
                                </div>
                            </div>
                            <% if (game.is_featured && listType !== 'featured') { %>
                            <div class="absolute top-2 left-2 z-10">
                                <span class="bg-gradient-to-r from-yellow-400 to-orange-500 text-white text-xs font-bold px-2 py-1 rounded-full shadow-md border border-yellow-300/50">
                                    ⭐ <%= __('games.featured') %>
                                </span>
                            </div>
                            <% } %>
                        </div>
                    </a>
                <% }); %>
            </div>

            <!-- Pagination (Bottom) -->
            <div class="flex justify-center mt-8">
                <nav class="inline-flex items-center space-x-1" aria-label="Pagination">
                    <% if (currentPage > 1) { %>
                        <a href="<%= baseUrl %>/page/<%= currentPage - 1 %>" class="inline-flex items-center gap-2 rounded-full bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 border border-gray-300 dark:border-gray-600 font-medium" system-nav>
                            <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                            </svg> <%= __('pagination.previous') %>
                        </a>
                    <% } else { %>
                        <span class="inline-flex items-center gap-2 rounded-full bg-gray-100 dark:bg-gray-700 text-gray-400 dark:text-gray-500 px-4 py-2 font-medium">
                            <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                            </svg> <%= __('pagination.previous') %>
                        </span>
                    <% } %>
                    
                    <span class="px-3 py-2 text-gray-600 dark:text-gray-400"><%= __('pagination.page') %> <%= currentPage %></span>
                    
                    <% if (hasNextPage) { %>
                        <a href="<%= baseUrl %>/page/<%= currentPage + 1 %>" class="inline-flex items-center gap-2 rounded-full bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 border border-gray-300 dark:border-gray-600 font-medium" system-nav>
                            <%= __('pagination.next') %> <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                            </svg>
                        </a>
                    <% } else { %>
                        <span class="inline-flex items-center gap-2 rounded-full bg-gray-100 dark:bg-gray-700 text-gray-400 dark:text-gray-500 px-4 py-2 font-medium">
                            <%= __('pagination.next') %> <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                            </svg>
                        </span>
                    <% } %>
                </nav>
            </div>
            
            <!-- Footer Banner Ad -->
            <div class="flex justify-center mt-8">
                <% if (enable_frontend_pjax === '0') { %>
                    <!-- Direct ads for AdSense compatibility when PJAX is disabled -->
                    <%- include('../modules/ad_direct.block.ejs', { placement: 'footer-banner', width: 728, height: 90, classes: '!hidden lg:!block mx-auto' }) %>
                    <!-- Mobile footer ad (sidebar rectangle) -->
                    <%- include('../modules/ad_direct.block.ejs', { placement: 'sidebar-rectangle', width: 300, height: 250, classes: '!block lg:!hidden mx-auto' }) %>
                <% } else { %>
                    <!-- AJAX ads for smooth navigation when PJAX is enabled -->
                    <%- include('../modules/ad_container.block.ejs', { placement: 'footer-banner', width: 728, height: 90, classes: '!hidden lg:!block mx-auto' }) %>
                    <!-- Mobile footer ad (sidebar rectangle) -->
                    <%- include('../modules/ad_container.block.ejs', { placement: 'sidebar-rectangle', width: 300, height: 250, classes: '!block lg:!hidden mx-auto' }) %>
                <% } %>
            </div>
            
            <% } else { %>
            <!-- No Games Found -->
            <div class="text-center py-16">
                <div class="max-w-lg mx-auto">
                    <!-- Icon with gradient background -->
                    <div class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-br from-blue-100 dark:from-blue-900/50 to-purple-100 dark:to-purple-900/50 rounded-full mb-6">
                        <svg class="w-10 h-10 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h1.01M15 10h1.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                    </div>
                    
                    <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100 mb-3"><%= __('games.no_games_found') %></h3>
                    <p class="text-gray-600 dark:text-gray-400 mb-8 leading-relaxed">
                        <% if (listType === 'recent') { %>
                            <%= __('games.no_recent_games_message') %>
                        <% } else if (listType === 'favorites') { %>
                            <%= __('games.no_favorites_message') %>
                        <% } else { %>
                            <%= __('games.no_category_games_message') %>
                        <% } %>
                    </p>
                    
                    <!-- Prettier Browse All Games button -->
                    <a href="/games" class="group inline-flex items-center gap-3 bg-gradient-to-r from-blue-600 to-purple-600 text-white px-8 py-4 rounded-2xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1 hover:from-blue-700 hover:to-purple-700" system-nav>
                        <div class="flex items-center justify-center w-8 h-8 bg-white/20 rounded-lg group-hover:bg-white/30 transition-colors duration-200">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                            </svg>
                        </div>
                        <span><%= __('games.browse_all_games') %></span>
                        <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                    </a>
                    
                    <!-- Optional: Additional quick links -->
                    <div class="mt-8 flex flex-wrap justify-center gap-3">
                        <a href="/games/popular" class="inline-flex items-center gap-2 text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 px-4 py-2 rounded-lg hover:bg-blue-50 dark:hover:bg-blue-900/30 transition-colors duration-200 font-medium" system-nav>
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10c0-2 .5-5 2.986-7C14 5 16.09 5.777 17.656 7.343A7.975 7.975 0 0120 13a7.975 7.975 0 01-2.343 5.657z"></path>
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.879 16.121A3 3 0 1012.015 11L11 14l4 4-6.121-1.879z"></path>
                            </svg>
                            <%= __('games.popular') %>
                        </a>
                        <a href="/games/top" class="inline-flex items-center gap-2 text-purple-600 dark:text-purple-400 hover:text-purple-800 dark:hover:text-purple-300 px-4 py-2 rounded-lg hover:bg-purple-50 dark:hover:bg-purple-900/30 transition-colors duration-200 font-medium" system-nav>
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                            </svg>
                            <%= __('games.top_rated') %>
                        </a>
                        <a href="/games/trending" class="inline-flex items-center gap-2 text-green-600 dark:text-green-400 hover:text-green-800 dark:hover:text-green-300 px-4 py-2 rounded-lg hover:bg-green-50 dark:hover:bg-green-900/30 transition-colors duration-200 font-medium" system-nav>
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                            </svg>
                            <%= __('games.trending') %>
                        </a>
                    </div>
                </div>
            </div>
            <% } %>
        </main>
    </div>

    <%- include('../modules/footer.block.ejs', { page }) %>

<%- include('../footer.ejs', { page }) %>